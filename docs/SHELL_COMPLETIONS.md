# Shell completions

Shell completions are not generated yet.

A future implementation can use clap's completion support to generate completions for common shells. Until then, use `zlg help` and `zlg help <command>` for command discovery.
