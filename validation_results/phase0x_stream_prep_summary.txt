Phase 0x stream-decode prep summary

Updated Phase 0x to test streaming decode in grep.

Changes:
- added RawChunk::decode_streaming_lines
- added StreamDecodeOutcome
- added zlg grep --stream-decode
- grep stats now include stream_decode, crc_checked_chunks, and stream_early_stopped_chunks
- Phase 0x benchmark now runs both full and stream decode modes for zlg grep
- docs and Codex prompt updated

Current behavior:
- zlg cat still uses full selected chunk decode
- zlg grep default still uses full selected chunk decode
- zlg grep --stream-decode scans decoded lines from the zstd stream without storing the full uncompressed chunk in one Vec<u8>
- early stopped stream chunks may skip final CRC validation, and the stats expose that

No Cargo validation was run in ChatGPT.
